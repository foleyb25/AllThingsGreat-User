<%- partial('../../partials/screenplay-nav', {}) %>
<div class="container-fluid pt-5 pb-5">
    <div id="screenplay-review-details" class="screenplay-details-view" v-cloak>
        <div class="poster-box">
            <img :src="'https://image.tmdb.org/t/p/original'+screenplay.poster_path" alt="" class="screenplay-image-box">
        </div>
        <div class="title-box" v-if="screenplay.title">
            {{screenplay.title}}
        </div>
        <div class="horizontal-scroller">
            <div class="scroll-item" v-for="review in screenplay.reviews">
                <div class="scroll-item-container">
                    <div class="reviewer-box" >
                        <div class="circle-wrap" >
                            <div class="circle" :style="addkeyframes(review)">
                                <div class="mask full">
                                    <div class="fill" v-if="review.score > 8"></div>
                                    <div class="fill" v-if="review.score < 8 && review.score > 6"></div>
                                    <div class="fill" v-if="review.score < 6"></div>
                                </div>
                                <div class="mask half">
                                    <div class="fill" v-if="review.score > 8"></div>
                                    <div class="fill" v-if="review.score < 8 && review.score > 6"></div>
                                    <div class="fill" v-if="review.score < 6"></div>
                                </div>
                            </div>
                            <div class="inside-circle" :style="'background-image: url('+review.writer.profileImageUrl+'), url(/images/startup.png);'">
                                {{review.score}}
                            </div>
                        </div>
                    </div>
                    <div v-if="review.writer.nickName!=''"class="writer-name-box">
                        {{review.writer.nickName}}
                    </div>
                    <div v-if="review.writer.nickName==''"class="writer-name-box">
                        {{review.writer.fullName}}
                    </div>
                    <div class="review-date-box">
                        {{review.createdFromNow}}
                    </div>
                    <div v-if="review.blog_url!=''"class="blog-url-box">
                        <a :href="review.blog_url" target="_blank">Blog</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="divider-category">Additional Scores:</div>
        <hr class="divider">
        <div class="external-scores">
                <div v-if="screenplay.imdb_score!=''" class="external-score-box">
                    <a href="https://www.imdb.com/" target="_blank">
                        <img class="imdb-logo" src="/images/movie_website_logos/imdb_logo.png" alt="">
                    </a>
                    <div>{{screenplay.imdb_score}}</div>
                </div>
            
            
                <div v-if="screenplay.tmdb_score!=''"class="external-score-box">
                    <a href="https://www.themoviedb.org/" target="_blank">
                        <img class="tmdb-logo"src="/images/movie_website_logos/tmdb_logo.png" alt="">
                    </a>
                    <div>{{(screenplay.tmdb_score)*10}}%</div>
                </div>
            
            
                <div v-if="screenplay.metacritic_score!=''" class="external-score-box">
                    <a href="https://www.metacritic.com/" target="_blank">
                        <img class="metacritic-logo" src="/images/movie_website_logos/metacritic_logo.png" alt="">
                    </a>
                    <div>{{screenplay.metacritic_score}}</div>
                    
                </div>
            
            
                <div v-if="screenplay.rotten_score!=''"class="external-score-box">
                    <a href="https://www.rottentomatoes.com/" style="text-decoration: none;" target="_blank">
                        <img class="rotten-logo" src="/images/movie_website_logos/rotten_tomatoes_logo.png" alt="">
                    </a>
                    <div>{{screenplay.rotten_score}}</div>
                </div>
            
        </div>
        <div class="divider-category">Where To Watch:</div>
        <hr class="divider">
        <div v-if="screenplay.services.length > 0"class="horizontal-scroller">
            <div v-for="service in screenplay.services"class="scroll-item">
                <div class="scroll-item-container">
                    <a href="watch_url">
                        <img src="service.logo_url" alt="">
                    </a>
                </div>
            </div>
        </div>
        <div v-if="screenplay.services.length < 1">
            Not finding any streaming services here. Check out <span><a href="https://www.justwatch.com/" target="_blank">JustWatch</a></span> For more details
        </div>
        <div class="divider-category">Overview:</div>
        <hr class="divider">
        <div class="overview-box">
            <div>{{screenplay.overview}}</div>
        </div>
        <div class="divider-category">Details:</div>
        <hr class="divider">
        <div class="details-box">
            <div v-if="screenplay.runtime">Runtime: {{screenplay.runtime}} minutes</div>
            <div v-if="screenplay.seasons">Number of Seasons: {{screenplay.num_seasons}}</div>
            <div v-if="screenplay.revenue">Revenue: ${{screenplay.revenue}}</div>
            <div v-if="screenplay.homepage"><a :href="screenplay.homepage" style="font-style: underline; color:blue" target="_blank">Homepage</a></div>
            <div>Type: {{screenplay.media_type}}</div>
        </div>
        <hr class="divider">
        <div class="rating-box">
        </div>
    </div> 
</div>
<%- exposeLocalsToBrowser({keys: ['screenplay']}) %>

<style>
    [v-cloak] { display:none; }

    :root {
        --deg: 0deg;
        --bg-color: #ba2200;
        --bg-gradient: '',
  }

  .writer-name-box{
    height: 22%;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: .85rem;
  }

  .blog-url-box {
    height: 22%;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .review-date-box {
    height: 22%;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-style: italic;
    color: rgb(162, 162, 162);
  }

  .divider-category {
    text-align: left;
    width: 75%;
    height: auto;
    font-weight: 900;
    margin-top: 20px;
  }

  .imdb-logo {
    height: 25px;
    width: auto;
    margin-bottom: 10px;
  }

  .tmdb-logo {
    height: 25px;
    width: auto;
    margin-bottom: 10px;
  }

  .metacritic-logo {
    height: 25px;
    width: auto;
    margin-bottom: 10px;
  }

  .rotten-logo {
    height: 25px;
    width: auto;
    margin-bottom: 10px;
  }

  .external-scores {
    display: flex;
    flex-direction: row;
    width: 75%;
    justify-content: center;

  }

  .external-score-box {
    width: 25%;
    height: 100px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .external-score-box div {
    text-decoration: none !important;
  }

  .external-score-box:hover {
    /* Start the shake animation and make the animation last for 0.5 seconds */
    animation: external-score-box 0.5s;

    /* When the animation is finished, start again */
    animation-iteration-count: 1;
  }

    .screenplay-details-view {
        display: flex;
        flex-direction: column;
        text-align: center;
        align-items: center;
    }

    .poster-box {
        text-align: center;
        margin: 20px;

    }

    .screenplay-image-box {
        height: 450px;
        width: 300px;
        margin: auto;
        border: 2px solid black;
    }

    .title-box {
        height: 25px;
        width: 100%;
        text-align: center;
        font-size: 1.25rem;
        font-weight: 700;
    }

    .details-box {
        display: flex;
        flex-direction: row;
    }

    .details-box div {
        margin: 10px;
    }

    .overview-box {
        display: flex;
        flex-direction: column;
        text-align: left;
        margin: 10px;
        width: 69%;
    }

    .divider {
        border-top: 3px solid rgb(120, 120, 120);
        border-radius: 5px;
        margin: 25px;
        width: 75%;
    }

    .horizontal-scroller {
        background-color: rgb(255, 255, 255);
        overflow-y: hidden;
        overflow-x: auto;
        white-space: nowrap;
        height: 182px;
        align-items: center;
        margin: 25px auto;
        width: 75%;
    }

    .scroll-item {
      height: 100%;
      width: 125px;
      display: inline-block;
      justify-content: center;
      margin: 0px 5px;
      border: 2px solid black;
      border-radius: 8px;
    }

    .scroll-item-container {
        height: 100%;
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding-top: 5px;
    }

    .reviewer-box {
      position: relative;
      height: 50px;
      width: 50px;
    }

    .reviewer-image-box {
        height: 100%;
        width: 100%;
        border-radius: 100%;
    }
  
  .screenplay-container {
    display: inline-block;
    text-align: center;
    border: 1px solid rgb(0, 0, 0);
    border-radius: 8px;
    font-size: .5rem;
    margin: 2px;
    justify-content: center;
  }
  
  .circle-wrap {
    position: absolute;
    color:white;
    margin: auto;
    width: 50px;
    height: 100%;
    background: #cdcdcd;
    border-radius: 100%;
    border: 1px solid #000000;
  }

  
  .circle-wrap .circle .mask,
  .circle-wrap .circle .fill {
    width: 100%;
    height: 100%;
    position: absolute;
    border-radius: 100%;
    bottom: .1px;
  }
  
  .mask .fill {
    clip: rect(0px, 25px, 50px, 0px);
    background-image: var(--bg-gradient)
  }
  
  .mask.full,
  .circle .fill {
    animation: fill ease-in-out 3s;
    transform: rotate(var(--deg));
  }
  
  .circle-wrap .circle .mask {
    clip: rect(0px, 50px, 50px, 24px);
  }
  
  .circle-wrap .inside-circle {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: #d2eaf1;
    line-height: 40px;
    text-align: center;
    margin-top: 4px;
    margin-left: 4px;
    color: #dbdbdb;
    position: absolute;
    z-index: 100;
    font-weight: 700;
    font-size: 1.1em;
    background-repeat: no-repeat; 
    background-size: 50px 50px; 
    background-color: rgba(0, 0, 0, 0.27);
    background-blend-mode: multiply;
    background-position: center;
  }
  
  
  @keyframes fill{
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(var(--deg));
    }
  }

  @keyframes external-score-box {
    0% { transform: rotate(5deg); }
   25% { transform: rotate(-5deg); }
   50% { transform: rotate(5deg); }
   75% { transform: rotate(-5deg); }
  100% { transform: rotate(5deg); }
}
</style>